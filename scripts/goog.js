// Autogenerated by Specford.
var pg = null;
var Tester = require('../src/utils/tester');
var test = new Tester(pg);

var steps = [
function(status) {
if(status !== 'success') console.log('Network error');
else {
test.start();
test.textExists('header h1', 'Works For Your Mom');
//test.textDoesNotExist('body', 'Foo bar baz');
test.stop();
phantom.exit();
}}
];

var visit = function(url) {
if(pg) pg.close();
else pg = require('webpage').create();
pg.open(url, steps.shift());
};

visit('http://www.plangrid.com');
